import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projetoCursoIONIC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
